---
label: Blog post
hide_body: false
fields:
- type: text
  name: title
  label: Titre
  config:
    required: true
- type: datetime
  name: date
  label: Date de publication
  config:
    required: true
    display_utc: true
    date_format: DD/MM/YYYY
  default: now
- name: slug
  type: text
  config:
    required: true
  label: Url
  description: 'En minuscule, sans espace. ex: mon-blog-post'
